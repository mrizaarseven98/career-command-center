---
description: Synchronize Career Command Center scheduling with Claude
argument-hint: "[workspace path]"
---

Use the `career-command-center` skill to render the saved automation specification for `$ARGUMENTS`, or the active workspace when omitted. Prefer one persistent Claude Code Desktop local scheduled task because the workflow needs private local files. Update the existing task rather than duplicating it, and mark synchronization only after the task is confirmed.
