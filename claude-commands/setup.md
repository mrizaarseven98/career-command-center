---
description: Install or finish setting up Career Command Center
argument-hint: "[workspace path]"
---

Use the `career-command-center` skill to set up Career Command Center. Treat `$ARGUMENTS` as the preferred workspace path when provided. Initialize the workspace, install the native app with `--assistant-provider claude`, and complete as much of the evidence foundation as the user's current setup permits. Preserve all existing workspace data.
