---
description: Run the configured targeted job and PhD search now
argument-hint: "[workspace path]"
---

Use the `career-command-center` skill to run the configured search for `$ARGUMENTS`, or the active Career Command Center workspace when omitted. Research live sources broadly, verify canonical posting and application URLs, deduplicate against every lifecycle state, promote only evidence-aligned opportunities, and record the completed run through the bundled state CLI.
